name = "cloudflare-pages-demo"

# Cloudflare Pages configuration
# When deploying via Cloudflare Pages (Git integration), these settings
# are also configurable in the dashboard.

[build]
command = "npm run build"
output_dir = "dist"

# Environment variables can also be set here.
# However, it's recommended to use the Cloudflare Pages dashboard
# for sensitive values. Dashboard vars override these.

# [vars]
# VITE_APP_TITLE = "Cloudflare Pages Demo"
# VITE_API_URL = "https://api.production.example.com"
# VITE_FEATURE_FLAG = "enabled"
