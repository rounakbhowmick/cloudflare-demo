name = "cloudflare-pages-demo"
compatibility_date = "2026-02-15"

# Cloudflare Pages configuration for static assets
# This tells wrangler where to find the built files to deploy

[assets]
directory = "./dist"

# Environment variables can also be set here, but it's recommended
# to use the Cloudflare Pages dashboard for sensitive values.
# Dashboard vars override these.

# [vars]
# VITE_APP_TITLE = "Cloudflare Pages Demo"
# VITE_API_URL = "https://api.production.example.com"
# VITE_FEATURE_FLAG = "enabled"
